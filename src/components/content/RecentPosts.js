import React from 'react';

const RecentPosts = (props) => {
    if (props.allBlogs) {
    }

    return (
        <div id="recent_posts_container">

        </div>
    );
};

export default RecentPosts;